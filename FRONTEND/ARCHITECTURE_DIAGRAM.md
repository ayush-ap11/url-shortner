# Frontend Architecture Diagram

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                        USER INTERFACE                            │
├─────────────────────────────────────────────────────────────────┤
│  Pages Layer                                                     │
│  ├── HomePage          (Hero + Body + Footer)                   │
│  ├── LoginPage         (Email + Google + GitHub)                │
│  ├── RegisterPage      (Email + Google + GitHub)                │
│  ├── DashboardPage     (Protected)                              │
│  └── GithubCallbackPage (OAuth Handler)                         │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      ROUTING & GUARDS                            │
├─────────────────────────────────────────────────────────────────┤
│  ├── MainLayout        (Navbar + Content + Footer)              │
│  ├── AuthLayout        (Content only)                           │
│  ├── DashboardLayout   (Content only)                           │
│  ├── ProtectedRoute    (Auth required)                          │
│  └── PublicRoute       (Redirect if authenticated)              │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     STATE MANAGEMENT                             │
├─────────────────────────────────────────────────────────────────┤
│  Redux Store                                                     │
│  ├── authSlice                                                   │
│  │   ├── state: { user, isAuthenticated, isLoading, error }    │
│  │   └── actions: { loginUser, registerUser, logoutUser, ... } │
│  └── urlSlice                                                    │
│      ├── state: { urls, currentUrl, analytics, pagination }     │
│      └── actions: { createUrl, fetchUrls, updateUrl, ... }      │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                       SERVICE LAYER                              │
├─────────────────────────────────────────────────────────────────┤
│  API Services                                                    │
│  ├── authService    (login, register, logout, OAuth)            │
│  ├── urlService     (CRUD operations, analytics)                │
│  └── userService    (profile management)                        │
│                                                                  │
│  Axios Instance                                                  │
│  ├── Request interceptor   (add auth token)                     │
│  └── Response interceptor  (handle token refresh)               │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      BACKEND API                                 │
├─────────────────────────────────────────────────────────────────┤
│  Auth Endpoints                                                  │
│  ├── POST /auth/login                                            │
│  ├── POST /auth/register                                         │
│  ├── POST /auth/google                                           │
│  ├── POST /auth/github/callback                                 │
│  └── POST /auth/refresh                                          │
│                                                                  │
│  URL Endpoints                                                   │
│  ├── POST /urls                                                  │
│  ├── GET /urls                                                   │
│  └── GET /urls/:id/analytics                                    │
└─────────────────────────────────────────────────────────────────┘
```

## Authentication Flow

### Email/Password Authentication
```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  User    │───▶│ LoginPage│───▶│authSlice │───▶│authSvc   │
│  Input   │    │ Form     │    │ dispatch │    │ API call │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       │
                                                       ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│Dashboard │◀───│ Redirect │◀───│ Store    │◀───│ Backend  │
│          │    │          │    │ Tokens   │    │ Response │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

### Google OAuth Flow
```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  User    │───▶│ Google   │───▶│ Google   │───▶│Credential│
│  Click   │    │ Popup    │    │ Auth     │    │ Token    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       │
                                                       ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│Dashboard │◀───│ Store    │◀───│ Backend  │◀───│authSlice │
│          │    │ Tokens   │    │ Validate │    │ dispatch │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

### GitHub OAuth Flow
```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  User    │───▶│ GitHub   │───▶│ GitHub   │───▶│ Code     │
│  Click   │    │ Redirect │    │ Auth     │    │ Param    │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       │
                                                       ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│Dashboard │◀───│ Store    │◀───│ Backend  │◀───│ Callback │
│          │    │ Tokens   │    │ Exchange │    │ Page     │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
```

## Token Refresh Flow

```
┌──────────┐    ┌──────────┐    ┌──────────┐
│ API Call │───▶│ 401      │───▶│ Axios    │
│ Fails    │    │ Response │    │ Intercept│
└──────────┘    └──────────┘    └──────────┘
                                      │
                                      ▼
                             ┌──────────────┐
                             │ Has Refresh? │
                             └──────────────┘
                                 │         │
                            Yes  │         │  No
                                 ▼         ▼
                         ┌──────────┐ ┌──────────┐
                         │ Refresh  │ │ Clear    │
                         │ Token    │ │ Auth &   │
                         │ API Call │ │ Redirect │
                         └──────────┘ └──────────┘
                                 │
                                 ▼
                         ┌──────────────┐
                         │ New Token?   │
                         └──────────────┘
                             │         │
                        Yes  │         │  No
                             ▼         ▼
                     ┌──────────┐ ┌──────────┐
                     │ Retry    │ │ Clear    │
                     │ Original │ │ Auth &   │
                     │ Request  │ │ Redirect │
                     └──────────┘ └──────────┘
```

## Data Flow

### Component ➜ Redux ➜ API ➜ Backend

```
┌─────────────────────────────────────────────────────────────────┐
│                    Component Layer                               │
│  ├── useAuth() hook        (read state)                         │
│  ├── useDispatch()         (dispatch actions)                   │
│  └── useSelector()         (select state)                       │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     Redux Layer                                  │
│  ├── dispatch(action)      (trigger async thunk)                │
│  ├── pending               (set loading state)                  │
│  ├── fulfilled             (update state with data)             │
│  └── rejected              (set error state)                    │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Service Layer                                 │
│  ├── authService.login()   (make API call)                      │
│  ├── axios interceptors    (add token, handle refresh)          │
│  └── return response       (or throw error)                     │
└─────────────────────────────────────────────────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Backend API                                   │
│  ├── Validate request      (check credentials)                  │
│  ├── Process               (business logic)                     │
│  ├── Generate tokens       (JWT)                                │
│  └── Send response         (user data + tokens)                 │
└─────────────────────────────────────────────────────────────────┘
```

## Folder Structure Visual

```
src/
│
├── 📁 components/          Reusable UI components
│   ├── Hero.jsx           Landing hero section
│   ├── Body.jsx           Features section
│   ├── Navbar.jsx         Navigation bar
│   ├── Footer.jsx         Footer section
│   └── Dashboard.jsx      Dashboard UI
│
├── 📁 pages/               Page-level components
│   ├── HomePage.jsx       Landing page
│   ├── LoginPage.jsx      Login form + OAuth
│   ├── RegisterPage.jsx   Registration form
│   ├── DashboardPage.jsx  User dashboard
│   └── GithubCallback.jsx OAuth callback handler
│
├── 📁 store/               Redux store
│   ├── index.js           Store configuration
│   └── slices/
│       ├── authSlice.js   Authentication state
│       └── urlSlice.js    URL management state
│
├── 📁 services/            API service layer
│   ├── api.js             Axios instance + interceptors
│   ├── authService.js     Auth API calls
│   ├── urlService.js      URL API calls
│   └── userService.js     User API calls
│
├── 📁 hooks/               Custom React hooks
│   ├── useAuth.js         Access auth state
│   └── useAuthInit.js     Initialize auth
│
├── 📁 utils/               Utility functions
│   ├── auth.js            Token management
│   ├── validation.js      Form validation
│   └── helpers.js         General utilities
│
├── 📁 routes/              Route protection
│   └── ProtectedRoute.jsx Auth guards
│
├── 📁 layouts/             Layout components
│   └── index.jsx          Layout wrappers
│
├── 📁 config/              Configuration
│   └── env.js             Environment vars
│
├── 📁 constants/           App constants
│   └── index.js           Endpoints, routes, keys
│
├── App.jsx                 Main app component
└── main.jsx                Entry point
```

## Component Composition

```
App
├── BrowserRouter
    ├── Routes
        ├── MainLayout (Public)
        │   ├── Navbar
        │   ├── HomePage
        │   │   ├── Hero
        │   │   └── Body
        │   └── Footer
        │
        ├── AuthLayout (Login/Register)
        │   ├── LoginPage
        │   │   ├── LoginForm
        │   │   ├── GoogleLogin
        │   │   └── GitHubButton
        │   ├── RegisterPage
        │   │   ├── RegisterForm
        │   │   ├── GoogleLogin
        │   │   └── GitHubButton
        │   └── GithubCallbackPage
        │
        └── DashboardLayout (Protected)
            └── DashboardPage
                └── Dashboard
                    ├── StatsCards
                    ├── RecentLinks
                    └── DomainForm
```

## State Management Architecture

```
Redux Store
├── auth
│   ├── user
│   │   ├── id
│   │   ├── name
│   │   ├── email
│   │   ├── phone
│   │   └── provider
│   ├── isAuthenticated
│   ├── isLoading
│   └── error
│
└── url
    ├── urls []
    │   ├── _id
    │   ├── originalUrl
    │   ├── shortCode
    │   ├── clicks
    │   └── createdAt
    ├── currentUrl
    ├── analytics
    │   ├── totalClicks
    │   ├── clicksByDate
    │   ├── referrers
    │   └── devices
    ├── isLoading
    ├── error
    └── pagination
        ├── page
        ├── limit
        └── total
```

## Security Layers

```
┌─────────────────────────────────────────┐
│  1. Input Validation (Frontend)        │
│     ✓ Email format                      │
│     ✓ Password strength                 │
│     ✓ Phone format                      │
└─────────────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│  2. Request Interceptor                 │
│     ✓ Add Authorization header          │
│     ✓ Add CSRF token                    │
└─────────────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│  3. Backend Validation                  │
│     ✓ Validate JWT token                │
│     ✓ Check permissions                 │
│     ✓ Sanitize inputs                   │
└─────────────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│  4. Response Interceptor                │
│     ✓ Handle 401 (refresh token)        │
│     ✓ Handle errors                     │
│     ✓ Clear auth on failure             │
└─────────────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│  5. Token Storage                       │
│     ✓ HttpOnly cookies                  │
│     ✓ localStorage fallback             │
│     ✓ Auto-expire                       │
└─────────────────────────────────────────┘
```

## Performance Optimization

```
┌─────────────────────────────────────────┐
│  Code Splitting                         │
│  ├── Route-based (automatic)            │
│  ├── Component lazy loading             │
│  └── Dynamic imports                    │
└─────────────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│  State Management                       │
│  ├── Redux normalized state             │
│  ├── Selector memoization               │
│  └── Component memoization              │
└─────────────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│  API Optimization                       │
│  ├── Request deduplication              │
│  ├── Response caching                   │
│  └── Pagination                         │
└─────────────────────────────────────────┘
                  ▼
┌─────────────────────────────────────────┐
│  Bundle Optimization                    │
│  ├── Tree shaking                       │
│  ├── Minification                       │
│  └── Compression (gzip/brotli)          │
└─────────────────────────────────────────┘
```

---

## Key Architecture Decisions

### ✅ Why Redux Toolkit?
- Simplified Redux API
- Built-in thunks and immer
- DevTools integration
- Best practices enforced

### ✅ Why Service Layer?
- Separation of concerns
- Reusable API calls
- Centralized error handling
- Easy to test

### ✅ Why Route Guards?
- Automatic redirects
- Consistent auth checks
- Cleaner route definitions
- Better UX

### ✅ Why Custom Hooks?
- Reusable logic
- Cleaner components
- Easier testing
- Better composition

### ✅ Why Token Refresh?
- Better UX (no sudden logouts)
- Automatic recovery
- Secure (short-lived tokens)
- Industry standard

---

**This architecture is production-ready, scalable, and maintainable.**
